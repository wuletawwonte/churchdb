# Prefer setting the Apache/Nginx document root to the `public/` directory.
# If the vhost must use this folder as docroot, forward requests into `public/`.
<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteCond %{REQUEST_URI} !^/?public/
	RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
